# EchoCat Skill Panel 4.4.0 — 背景自定义

## 新增功能

- 真实横栏与中心报告右上角增加“背景自定义”；宽屏显示文字，窄屏为带提示的图标。
- 六套低饱和预设，支持液态、柔和渐变、纯色和本地图片。
- 主色、辅助色、高光色、明暗、亮度、饱和度、对比、高光、柔化、遮罩、速度、形变与指针反馈均实际作用于背景；不适用的选项会隐藏。
- 图片支持选择、更换、移除、铺满裁切/完整显示/拉伸及位置。接受 JPG/PNG/WebP，上限 8 MB，在本机缩至长边 1920 像素以内，并单独保存在客户端数据库中，不上传。
- 设置跨本插件表面共享，刷新后保留；实时预览、取消与恢复默认明确分开，保存失败会提示。只限当前客户端，不宣称跨设备同步。

## 保留与验证

保留 4.3.1 连续液态表面、卡片层级、居中布局、滚动和技能业务。静态模式不持续渲染；隐藏、卸载和减少动态仍受支持。设置使用独立 IndexedDB 空间，不修改技能数据、宿主配置或凭据流程。

相关回归、设置边界测试和真实 React 组件后台离线浏览器验证；最终数字见交付说明。未安装到运行中的 DSH、未重启或操作用户桌面、未发布。真实 DSH 安装验收由用户完成。
