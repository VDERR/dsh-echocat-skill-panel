# EchoCat Skill Panel 5.1.3 — 修复细线横栏与无标签数字

## 用户可见修复

- 新会话里横栏不再被宿主压缩成一条只有背景的细线。
- DSH 窗口缩窄后，四个统计项仍会显示“回合 / 用到 / 未用 / 调用”，不会只剩数字。
- 窄栏继续保持一行，右侧按钮不会折到第二行、重叠或越界。
- 宽度足够时仍显示完整统计名称和“背景自定义”文字按钮。

## 实现

- 为横栏外壳、横栏行和条本体设置 48px / 46px 的不可压缩最小高度。
- 窄栏改为左侧摘要弹性收缩、右侧统计与操作区按内容取自然宽度；最窄一档由装饰图标让位，而不是删除信息标签。
- 操作按钮组在所有宽度下都禁止换行。
- 紧凑断点调整为 1240px，覆盖 DSH 保存的自定义会话宽度，不再只按默认 composer 上限判断。

## 回归门

真实组件套入 DSH hero composer 结构，在八档宿主宽度上检查：外壳高度、四个统计标签可见性、单行布局、横向/纵向溢出、右侧重叠和整体越界。八档全部通过后才允许打包。
