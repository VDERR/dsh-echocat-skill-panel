# EchoCat Skill Panel 5.1.0 — 横栏图标可以隐藏了

## 新增

- **「背景自定义」里多了一个「横栏图标」开关**：显示 / 隐藏输入框上方横栏中间那只猫标。
- **隐藏后图标的位置会保留**，横栏里的四个计数**不会移位**。横栏是三列布局（左结论 / 中间图标 / 右计数），中间那列是**固定的 22px**，靠它把结论居中。所以隐藏用的是 `visibility` 而不是 `display:none` —— 后者会把这一列收掉，**每拨一次开关结论就横跳一次**。实测：显示与隐藏两种状态下，结论的左边缘都是 905px。
- **设置面板里的实时预览也放了一只猫标**，拨开关**当场就能看到效果** —— 否则被控制的那只标在面板背后，拨了没有任何反应。

## 没有动的东西

按你的要求，**没有破坏现有结构**，也没有引入新组件：

- 开关**复用**上面「背景样式」那排按钮的同一套实现 —— 同 `sr-bg-modes` 容器、同 `sr-btn` 按钮、同 `aria-pressed` 状态。测试逐字比对了两个表达式，确认构造完全一致，并且这些 class 在样式表里**确实有规则**（没有规则的新控件才会显得"贴上去的"）。
- 新开关放进**已有的 `sr-bg-section`**、**已有的 `fieldset`**（保存中会一起禁用），文案用**已有的 `sr-bg-help`**。
- 设置存在**同一个** IndexedDB 记录里，跟着「保存背景」一起生效 / 取消。
- 取值只有 `show` / `hide` 两个，非法值一律回落成 `show`；**`true` / `0` 这类"看起来像布尔"的值不会被误当成状态**（有测试）。

## 顺手修掉的一个隐患

猫标原本定义在 `panel.js` 里。设置面板要用同一个标，而 `panel.js` 本来就 `require('./background-panel.js')` —— **反向引用会形成循环依赖**。它当时恰好能跑，只是因为函数声明会提升；哪天改成 `const` 箭头函数就会断。所以把标提取成**独立叶子模块 `logo-mark.js`**，两边都引用它：**把问题消掉，而不是依赖它碰巧不出事**。

## 验证

- 断言从 **1550 条增加到 1571 条**，新增覆盖：设置默认值与两种合法值、非法值与"像布尔的值"的回落、CSS 里隐藏规则存在、**以及隐藏没有把元素移出布局**（明确断言不用 `display:none`）。
- **新断言是反向验证过的**：把 `visibility:hidden` 换成 `display:none` 后，两条断言立刻失败（529 → 527），确认它们真的能抓到那个错误，而不是摆设。
- 全部测试、浏览器门 14/14、十二个门全绿。

## 安装

```powershell
dsh plugin --profile web add dsh-echocat-skill-panel
```

装完**重启** DSH Desktop Beta。已保存的背景设置和本地图片都不受影响。
