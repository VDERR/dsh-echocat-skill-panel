# EchoCat Skill Panel 5.1.2 — 修复新会话横栏换行与裁切

## 现象

新会话的输入框上方横栏看起来是全宽，但计数、摘要或操作按钮会被挤到第二行；宿主的 composer dock 按单行控件布局时，第二行会被裁掉，表现成文字只剩半截或界面没有随宽度自适应。

## 根因

5.1.1 修复了匿名容器查询，却只验证了 `.sr-strip-shell` 外框的宽度。真实横栏内部仍有两个冲突：

- 右半栏同时容纳四个计数、展开箭头、`背景自定义` 和其余操作按钮，固定内容总宽度超过了半栏。
- `.sr-strip-right` 与 640px 容器规则允许/强制换行，用第二行掩盖了宽度冲突。

所以旧的宽度契约可以 57/57 全绿，真实组件仍然是两行。

## 修复

- 横栏和右半栏始终保持单行，不再以换行处理冲突。
- 在宿主 936px composer 上限内，`背景自定义` 自动收成保留完整 `aria-label` 与悬停标题的图标按钮。
- 四个计数在常规宽度显示短标签；在宿主最窄两档只保留数值，完整标签仍保留在每个计数的 `title` 与 `aria-label` 中。
- 收紧计数、按钮与间距，但保留摘要省略、中央 EchoCat 标志、四个计数和全部操作功能。

## 新回归门

新增真实 Chromium 测量 `tools/check-strip-responsive.mjs`，直接渲染插件自己的组件和最终样式，在宿主四档宽度下检查：

- 横栏只有一行；
- 横向、纵向均不溢出；
- 右半栏内部不重叠、不越界；
- 所有子项都留在横栏边界内。

这项检查在 5.1.1 上四档全部失败，在 5.1.2 上全部通过。

## 安装

解压后运行 `安装.ps1`，再从托盘完全退出并重新打开 DSH Desktop Beta。已保存的背景设置、本地图片与其他插件数据不受影响。
