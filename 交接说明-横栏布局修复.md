# 横栏布局修复交接说明（5.1.2）

## 修复内容

- 根因不是横栏外壳宽度，而是右半区的统计项、背景按钮和操作按钮总宽度超过可用空间，同时旧样式允许 `flex-wrap`，因此操作按钮被挤到第二行并被 DSH 输入区裁切。
- 右半区现在始终保持单行；在较窄的宿主宽度下，背景按钮收成图标，统计项使用短标签；极窄宽度只隐藏统计文字标签，数值仍保留。
- 四个统计项增加了 `aria-label` 和 `title`，压缩显示时仍可识别含义。

## 回归验证

- 原有 12 组测试门全部通过。
- 真实组件在 Chromium 中按 648、712、851、952 四档 DSH 宿主宽度验证：均为单行、无水平或垂直溢出、右侧按钮未重叠且没有越界。
- 输入框横栏宽度回归：57/57 通过。
- 版本：`5.1.2`；已构建客户端：`lib/client.js`。

## 安装边界

本包只是在隔离副本中修复和打包，没有改动、安装或重启现有 DSH。请解压后由使用者运行 `安装-3.0.ps1`，随后完整退出并重新打开 DSH Desktop Beta 进行实机验收。
