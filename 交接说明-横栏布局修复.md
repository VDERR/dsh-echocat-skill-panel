# 横栏布局修复交接说明（5.1.3）

## 修复内容

- 5.1.2 在宽度小于 720px 时主动隐藏了统计标签，导致右侧只剩四个没有含义的数字。5.1.3 不再隐藏标签，窄栏始终显示“回合 / 用到 / 未用 / 调用”。
- 窄栏不再强制左右等宽：右侧按内容取自然宽度，左侧摘要负责缩短；装饰性的中间图标只在最窄一档让位。
- 横栏外壳、行和条本体都增加不可压缩的最小高度，防止新会话宿主布局把横栏压成一条细线。
- 操作按钮组从源头禁止换行；完整标签和“背景自定义”仅在 1240px 以上启用，断点由实际内容宽度计算，不再按默认 936px 宿主上限猜测。

## 回归验证

- 原有测试门全部通过。
- 真实组件套入 DSH 新会话的 hero 宿主结构，并覆盖 648、712、851、952、1040、1258、1259、1304 八档宿主宽度。
- 每档都必须同时满足：横栏不低于 48px、行不低于 46px、四个标签均可见、单行、无横向或纵向溢出、按钮不重叠且不越界。
- 输入框横栏宽度回归：57/57 通过。
- 版本：`5.1.3`；已构建客户端：`lib/client.js`。

## 安装边界

本包只是在隔离副本中修复和打包，没有改动、安装或重启现有 DSH。请解压后由使用者运行 `安装-3.0.ps1`，随后完整退出并重新打开 DSH Desktop Beta 进行实机验收。
